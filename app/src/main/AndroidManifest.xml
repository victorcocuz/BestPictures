<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.bestpictures">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.example.android.bestpictures.MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.android.bestpictures.DetailActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:parentActivityName="com.example.android.bestpictures.MainActivity"
            android:theme="@style/DetailActivityTheme" />
        <activity
            android:name="com.example.android.bestpictures.SettingsActivity"
            android:parentActivityName=".MainActivity" />

        <provider
            android:name=".data.BestPicturesProvider"
            android:authorities="com.example.android.bestpictures"
            android:exported="false" />
    </application>

</manifest>